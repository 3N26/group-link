{
  "name": "Group Links",
  "module": "src/index.ts",
  "type": "module",
  "description": "Group similar links and navigate them in tab pool by shortcut",
  "version": "0.0.1",
  "scripts": {
    "watch:build": "bun ./scripts/watchBuild.ts",
    "build": "bun ./scripts/build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}